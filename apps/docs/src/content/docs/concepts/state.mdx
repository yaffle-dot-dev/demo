---
title: State
description: How Yaffle manages OpenTofu state
---

import { Aside } from '@astrojs/starlight/components';

Yaffle fully manages OpenTofu state. You don't configure backends, set up S3 buckets, or manage DynamoDB tables. State storage and locking are handled automatically.

## How it works

When Yaffle runs OpenTofu, it configures the backend automatically. Any backend configuration in your code is ignored during Yaffle runs.

**Your OpenTofu (ignored by Yaffle):**
```hcl
terraform {
  backend "s3" {
    bucket = "my-existing-bucket"
    key    = "terraform.tfstate"
  }
}
```

Your local development can still use your own backend. Yaffle only manages state for runs it executes.

## State isolation

Each environment and preview gets isolated state:

| Context | State isolation |
|---------|-----------------|
| Production | Dedicated state per workspace |
| Staging | Dedicated state per workspace |
| Preview (PR #42) | Ephemeral state, destroyed with PR |

Preview state is completely isolated from production. A bug in a preview can't corrupt production state.

## Locking

Yaffle prevents concurrent modifications. If two commits push simultaneously, runs are queued and executed sequentially.

## Lifecycle

| Event | State action |
|-------|--------------|
| PR opened | State created on first apply |
| PR updated | State updated |
| PR merged/closed | State destroyed with resources |
| Push to main | Production state updated |

<Aside type="caution">
Preview state is ephemeral. When a PR closes, Yaffle destroys the preview resources and deletes the state. Don't store anything permanent in previews.
</Aside>

## Security

- Encrypted at rest
- Encrypted in transit
- Isolated per organization
- No customer access to raw state files

## Viewing state

The Yaffle dashboard shows:

- Resource counts per workspace
- Last modified timestamps
- Drift detection (planned)

For debugging, use `tofu show` output in run logs rather than accessing state directly.

## Migrating existing state

If you have existing state you want Yaffle to manage:

1. Import your OpenTofu/Terraform directory as a workspace
2. Yaffle runs `tofu plan` against empty state
3. Use `tofu import` commands to bring resources under management

Or continue managing existing infrastructure separately and use Yaffle for new workspaces.
